#include "main.h"

/**
* main - check the code
* print_alphabet - print alphabet in lower case
*
* Return: Always 0.
*/
void print_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		_putchar(i);
	}

	return (0);
}
